# -*- mode: tqfolder -*-
@CreateModels/?/Samples {
  # default ggf
  +sigGGF {
    <Type = S, Path = "sig/[em$(Variation)+me$(Variation)]/$(campaign)/ggf">

  }

  # default vbf
  +sigVBF {
    <Type = S, Path = "sig/[em$(Variation)+me$(Variation)]/$(campaign)/vbf">

  }

  # default other higgs
  +otherH {
    <Type = B, Path = "sig/[em$(Variation)+me$(Variation)]/$(campaign)/[vh+htt]">
  }

  # add the muGGF
  @sigGGF {
    +NormFactor.muGGF {
      <Val = 1., Low = -50., High = 50.>
    }
  }

  # add the muVBF
  @sigVBF {
    +NormFactor.muVBF {
      <Val = 1., Low = -50., High = 50.>
    }
  }
}
